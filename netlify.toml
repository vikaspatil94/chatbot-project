[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--prefix=."

[dev]
  framework = "vite"
  targetPort = 5173
  command = "npm run dev"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
